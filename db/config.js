/* 配置 */

var config = {
    database: {
        DATABASE: "abc", //数据库名称
        USERNAME: "root", //数据库登录名
        PASSWORD: "", //密码
        PORT: "3306", //端口号
        HOST: "localhost" //服务器ip
    }
}

module.exports = config;